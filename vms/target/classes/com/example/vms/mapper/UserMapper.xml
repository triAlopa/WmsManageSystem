<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.vms.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.vms.entity.User">
            <id property="id" column="id" />
            <result property="no" column="no" />
            <result property="name" column="name" />
            <result property="password" column="password" />
            <result property="age" column="age" />
            <result property="sex" column="sex" />
            <result property="phone" column="phone" />
            <result property="roleId" column="role_id" />
            <result property="isValid" column="isValid" />
    </resultMap>




    <sql id="Base_Column_List">
        id,no,name,password,age,sex,
        phone,role_id,isValid
    </sql>
    <select id="PageC" resultType="com.example.vms.entity.User" >
        select *
        from user
    </select>
    <select id="PageCC" resultType="com.example.vms.entity.User">
        select *
        from user ${ew.customSqlSegment}
    </select>
    <select id="PageSelectAdmin" resultType="com.example.vms.entity.User">
        select *
        from user where role_id=1
    </select>
</mapper>
