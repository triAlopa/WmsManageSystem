<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vms.mapper.RecordMapper">

    <resultMap id="BaseResultMap" type="com.example.vms.entity.RecordRes">
        <id property="id" column="id"/>
        <result property="goods" column="goods"/>
        <result property="userid" column="userId"/>
        <result property="adminId" column="admin_id"/>
        <result property="count" column="count"/>
        <result property="createtime" column="createtime"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,goods,userId,admin_id,count,createtime,
        remark
    </sql>

    <select id="pageC" resultType="com.example.vms.entity.RecordRes">
        SELECT r.*,g.name goodsname,s.name storagename,gt.name goodsTypename,
               (SELECT u.name FROM user u WHERE u.id = r.userId)   username,
               (SELECT u.name FROM user u WHERE u.id = r.admin_id) adminname
          from record r,goods g,storage s,goodstype gt
--         WHERE g.id=r.goods AND g.storage=s.id AND g.goodsType=gt.id
        ${ew.customSqlSegment}
    </select>
</mapper>
